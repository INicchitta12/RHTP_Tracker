# 06_build_workbook.R -----------------------------------------------------------
# Stage 6 — Excel deliverable — build spec §11
#
# CONTRACT: openxlsx workbook, nine sheets, Tier 1/2/3 physically separated.
#
# STATUS: Not started.
#
# Conventions (CLAUDE.md §3): tidyverse, %>% only — never |>. snake_case.
# Explicit dplyr:: namespacing. No setwd(); use here::here().
#
# source(here::here("R", "utils_config.R")) for config, paths, credentials,
# and quota handling.

stop(
  "06_build_workbook.R is not implemented yet. See CLAUDE.md \xc2\xa710 (Current state) ",
  "and docs/stage0_preflight_findings.md.",
  call. = FALSE
)
